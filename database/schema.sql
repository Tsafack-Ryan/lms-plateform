CREATE DATABASE IF NOT EXISTS lms_plateforme CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE lms_plateforme;

-- Sous-compte : ce fichier est un doublon de config/schema.sql
-- Veuillez utiliser config/schema.sql comme source unique
-- Ce fichier est conservé pour compatibilité et redirige vers config/schema.sql

SOURCE ../config/schema.sql;